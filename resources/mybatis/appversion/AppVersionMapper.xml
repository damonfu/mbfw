<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AppVersionMapper">

	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select a.id,a.packageName,a.versionCode,a.versionName,a.used,a.url,a.content,a.createTime,a.state
		from tb_app_version a
		where 1 = 1
		<if test="pd.KEYW != null and pd.KEYW != ''"><!-- 关键词检索 -->
			and
			(
			a.content LIKE CONCAT(CONCAT('%', #{pd.KEYW}),'%')
			)
		</if>
		order by createTime desc
	</select>

	<delete id="delete" parameterType="pd">
		delete from tb_app_version
		where
			id = #{id}
	</delete>

	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from tb_app_version
		where
		id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
			#{item}
		</foreach>
	</delete>

	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select
		id,
		packageName,
		versionCode,
		versionName,
		used,
		url,
		content,
		createTime,
		state
		from
			tb_app_version
		where
			id = #{id}
	</select>

	<select id="findLaste" parameterType="pd" resultType="AppVersionBean">
		select
		id,
		packageName,
		versionCode,
		versionName,
		used,
		url,
		content,
		createTime,
		state
		from
			tb_app_version
		where
			packageName = #{packageName}
			and
	  		used = 1
	  		and
	  		versionCode > #{versionCode}
	  	order by createTime desc limit 1
	</select>

	<insert id="save" parameterType="pd">
		insert into tb_app_version (
		id,
		packageName,
		versionCode,
		versionName,
		used,
		url,
		content,
		createTime,
		state
		) values (
		#{ID},#{PACKAGENAME},#{VERSIONCODE},#{VERSIONNAME},#{USED},#{URL},#{CONTENT},#{CREATETIME},#{STATE}
		)
	</insert>

	<update id="edit" parameterType="pd">
		update tb_app_version set
			packageName=#{PACKAGENAME},
			versionCode=#{VERSIONCODE},
			versionName=#{VERSIONNAME},
			used=#{USED},
			url=#{URL},
			content=#{CONTENT},
			state=#{STATE}
		where
			id=#{ID}
	</update>

</mapper>